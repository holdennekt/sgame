root = "."
test_exited = false

[build]
  cmd = "CGO_ENABLED=0 go build -o ./tmp/main ./cmd/server/main.go"
  bin = "./tmp/main"
  delay = 1000
  exclude_dir = ["tmp", "vendor"]
  include_ext = ["go", "tpl", "tmpl", "html", "env"]

[log]
  time = true
